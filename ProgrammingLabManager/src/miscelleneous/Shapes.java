package miscelleneous;

import javafx.scene.shape.Polygon;

public class Shapes
{
	public static Polygon drawPolygonalDirectionSign(int size)
	{
		Polygon p=new Polygon(0*size,0*size,0*size,34*size,10*size,17*size);
		return p;
	}
}
